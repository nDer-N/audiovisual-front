import { Typography } from '@mui/material';
import { useAppContext } from '../context/AppContext';
export default function ReservarEquipo() {
  return <Typography variant="h5">Página para reservar equipo</Typography>;
}
