import { Typography } from '@mui/material';
import { useAppContext } from '../context/AppContext';
export default function MisReservas() {
  return <Typography variant="h5">Tus reservas actuales</Typography>;
}
